#include <stdio.h>
#include <limits.h>

int main() {
    printf("int max: %d\n", INT_MAX);
    printf("long max: %ld\n", LONG_MAX);
    printf("long long max: %lld\n", LLONG_MAX);
    printf("unsigned long long max: %llu\n", ULLONG_MAX);
    return 0;
}

